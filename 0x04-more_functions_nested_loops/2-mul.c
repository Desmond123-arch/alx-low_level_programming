#include "main.h"
#include <stdio.h>
/**
 * mul - prints the product of two integers
 * @a: first number
 * @b: second number
 * Return: 1(success)
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
