#include "main.h"
/**
 * add - adds two integers and returns result
 * @n: first number
 * @l: second number
 *
 * Return: sum of two arguments
*/
int add(int n, int l)
{
	int sum = n + l;

	return (sum);
}
